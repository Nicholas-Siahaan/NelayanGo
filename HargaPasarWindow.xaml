<Window x:Class="NelayanGo.HargaPasarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NelayanGo.ViewModels"
        mc:Ignorable="d"
        Title="NELAYAN GO - Harga Pasar" Height="700" Width="1200"
        Background="#F0F0F0">

    <Window.DataContext>
        <vm:HargaPasarViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Style untuk efek bayangan sederhana -->
        <DropShadowEffect x:Key="ShadowDepth1" BlurRadius="5" ShadowDepth="2" Direction="270" Opacity="0.1"/>

        <!-- Style untuk Header DataGrid -->
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="#E6F2FF"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <!-- Style untuk Cell DataGrid -->
        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#C2E0FF"/>
                    <Setter Property="Foreground" Value="#333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style untuk Judul Utama Section -->
        <Style TargetType="{x:Type TextBlock}" x:Key="SectionHeader">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- 0: Header -->
            <RowDefinition Height="*"/>
            <!-- 1: Konten Utama -->
        </Grid.RowDefinitions>

        <!-- === HEADER (ROW 0) === -->
        <Grid Grid.Row="0" Background="#0361A0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <TextBlock Text="N" FontFamily="Segoe UI" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="NELAYAN GO" FontFamily="Segoe UI" Foreground="White" FontSize="20" FontWeight="SemiBold"/>
            </StackPanel>

            <!-- Navigasi -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Home" Foreground="White" VerticalAlignment="Center" Margin="20,0,20,0"/>
                <TextBlock Text="Hasil Tangkapan" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBlock Text="Maps" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBlock Text="Harga Pasar" Foreground="White" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,20,0"/>
            </StackPanel>

            <!-- Info Pengguna -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,0">
                <Border CornerRadius="50" Width="30" Height="30" Background="White" VerticalAlignment="Center" Margin="0,0,10,0">
                    <TextBlock Text="👤" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,2"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="FIKRI HOIRUL" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="TGL-32" Foreground="White" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- === KONTEN UTAMA (ROW 1) === -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <!-- Kolom Kiri: Daftar Harga -->
                <ColumnDefinition Width="*"/>
                <!-- Kolom Kanan: Profit Tangkapan -->
            </Grid.ColumnDefinitions>

            <!-- KIRI: DAFTAR HARGA IKAN -->
            <StackPanel Grid.Column="0" Margin="0,20,30,20">
                <TextBlock Text="DAFTAR HARGA IKAN" Style="{StaticResource SectionHeader}"/>

                <Border Background="White" CornerRadius="10" Effect="{StaticResource ShadowDepth1}">
                    <DataGrid ItemsSource="{Binding DaftarHarga}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              IsReadOnly="True"
                              GridLinesVisibility="None"
                              BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="NAMA IKAN" Binding="{Binding NamaIkan}" Width="*"/>
                            <DataGridTextColumn Header="HARGA PER KILO" Binding="{Binding HargaPerKilo, StringFormat=N0}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>

            <!-- KANAN: PROFIT TANGKAPAN HARI INI -->
            <StackPanel Grid.Column="1" Margin="30,20,0,20">
                <TextBlock Text="PROFIT TANGKAPAN HARI INI" Style="{StaticResource SectionHeader}"/>

                <Border Background="White" CornerRadius="10" Effect="{StaticResource ShadowDepth1}" Padding="0,0,0,20">
                    <StackPanel>
                        <!-- Tabel Profit -->
                        <DataGrid ItemsSource="{Binding DaftarProfit}"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  IsReadOnly="True"
                                  GridLinesVisibility="None"
                                  BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="NAMA IKAN" Binding="{Binding NamaIkan}" Width="*"/>
                                <DataGridTextColumn Header="BERAT TANGKAPAN" Binding="{Binding BeratTangkapan, StringFormat=N2}" Width="*"/>
                                <DataGridTextColumn Header="HARGA" Binding="{Binding Harga, StringFormat=Rp. {0:N0}}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Total Pendapatan -->
                        <Border Background="#E6F2FF" Margin="20,20,20,0" CornerRadius="5" Padding="15">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="TOTAL PENDAPATAN HARI INI : " FontWeight="Bold" FontSize="16" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding TotalPendapatan, StringFormat=Rp. {0:N0}}" FontWeight="ExtraBold" FontSize="16" Foreground="#0361A0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>