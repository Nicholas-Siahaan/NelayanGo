<Window x:Class="NelayanGo.AnalisisWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NelayanGo.ViewModels"
        mc:Ignorable="d"
        Title="NELAYAN GO - Analisis Data" Height="900" Width="1200"
        Background="#F0F0F0">

    <Window.DataContext>
        <vm:AnalisisViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- Style untuk efek bayangan sederhana -->
        <DropShadowEffect x:Key="ShadowDepth1" BlurRadius="5" ShadowDepth="2" Direction="270" Opacity="0.1"/>

        <!-- Style untuk Judul Kartu -->
        <Style TargetType="{x:Type TextBlock}" x:Key="CardTitle">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- Style untuk Simulasi Label Sumbu X/Y Kecil -->
        <Style TargetType="{x:Type TextBlock}" x:Key="AxisLabel">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="#777"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
        </Style>

        <!-- Style Tombol Biru (Perbaikan CornerRadius) -->
        <Style TargetType="{x:Type Button}" x:Key="BlueButton">
            <Setter Property="Background" Value="#42A5F5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E88E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- 0: Header -->
            <RowDefinition Height="*"/>
            <!-- 1: Konten Utama -->
        </Grid.RowDefinitions>

        <!-- === HEADER (ROW 0) === -->
        <Grid Grid.Row="0" Background="#0361A0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <TextBlock Text="N" FontFamily="Segoe UI" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="NELAYAN GO" FontFamily="Segoe UI" Foreground="White" FontSize="20" FontWeight="SemiBold"/>
            </StackPanel>

            <!-- Navigasi -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Home" Foreground="White" VerticalAlignment="Center" FontWeight="Bold" Margin="20,0,20,0"/>
                <TextBlock Text="Hasil Tangkapan" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBlock Text="Maps" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBlock Text="Harga Pasar" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
            </StackPanel>

            <!-- Info Pengguna -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,0">
                <Border CornerRadius="50" Width="30" Height="30" Background="White" VerticalAlignment="Center" Margin="0,0,10,0">
                    <TextBlock Text="👤" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,2"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="FIKRI HOIRUL" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="TGL-32" Foreground="White" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- === KONTEN UTAMA (ROW 1) === -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- Kolom Kiri: Tangkapan Harian & Tahunan -->
                <ColumnDefinition Width="400"/>
                <!-- Kolom Kanan: Profit & Jenis Tangkapan -->
            </Grid.ColumnDefinitions>

            <!-- ============================== -->
            <!-- KIRI: GRAFIK HARIAN & TAHUNAN -->
            <!-- ============================== -->
            <Grid Grid.Column="0" Margin="0,10,30,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="300"/>
                    <!-- Tangkapan Hari Ini -->
                    <RowDefinition Height="*"/>
                    <!-- Tangkapan Setahun -->
                </Grid.RowDefinitions>

                <!-- KARTU 1: TANGKAPAN HARI INI (Grafik Garis Simulasi) -->
                <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="20" Effect="{StaticResource ShadowDepth1}" Margin="0,0,0,30">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <!-- Grafik -->
                            <ColumnDefinition Width="150"/>
                            <!-- Statistik -->
                        </Grid.ColumnDefinitions>

                        <!-- Judul dan Grafik Garis -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="TANGKAPAN HARI INI" Style="{StaticResource CardTitle}"/>
                            <!-- Placeholder Grafik Garis -->
                            <Border Height="170" BorderBrush="#eee" BorderThickness="1,0,0,1" Margin="0,0,10,0">
                                <TextBlock Text="[Grafik Garis Simulasi DataHarian]" Foreground="#bbb" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <!-- Label X Axis Simulasi -->
                            <Grid Margin="10,5,10,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource AxisLabel}" Text="19.00" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Style="{StaticResource AxisLabel}" Text="20.00" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Style="{StaticResource AxisLabel}" Text="21.00" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="3" Style="{StaticResource AxisLabel}" Text="22.00" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="4" Style="{StaticResource AxisLabel}" Text="23.00" HorizontalAlignment="Center"/>
                            </Grid>
                        </StackPanel>

                        <!-- Statistik Harian -->
                        <StackPanel Grid.Column="1" Margin="10,40,0,0">
                            <TextBlock Text="{Binding StatsHarian}" FontSize="12" LineHeight="18"/>
                            <Button Content="HASIL TANGKAPAN" 
                                    Style="{StaticResource BlueButton}"
                                    Margin="0,15,0,0"
                                    HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- KARTU 2: TANGKAPAN SETAHUN (Grafik Batang Simulasi) -->
                <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Effect="{StaticResource ShadowDepth1}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <!-- Grafik -->
                            <ColumnDefinition Width="150"/>
                            <!-- Statistik -->
                        </Grid.ColumnDefinitions>

                        <!-- Judul dan Grafik Batang Tahunan -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="TANGKAPAN SETAHUN" Style="{StaticResource CardTitle}"/>
                            <!-- Placeholder Grafik Batang -->
                            <Border Height="200" BorderBrush="#eee" BorderThickness="1,0,0,1" Margin="0,0,10,0">
                                <TextBlock Text="[Grafik Batang Simulasi DataTahunan]" Foreground="#bbb" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <!-- Label X Axis Simulasi -->
                            <TextBlock Text="Januari - Desember" Style="{StaticResource AxisLabel}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>

                        <!-- Statistik Tahunan -->
                        <StackPanel Grid.Column="1" Margin="10,40,0,0">
                            <TextBlock Text="{Binding StatsTahunan}" FontSize="12" LineHeight="18"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- ============================== -->
            <!-- KANAN: PROFIT & JENIS TANGKAPAN -->
            <!-- ============================== -->
            <StackPanel Grid.Column="1" Margin="30,10,0,10">
                <!-- KARTU 3: PROFIT HARIAN -->
                <Border Background="White" CornerRadius="10" Padding="20" Effect="{StaticResource ShadowDepth1}" Margin="0,0,0,30">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="18" FontWeight="SemiBold" Text="Profit Harian : "/>
                        <TextBlock Grid.Column="0" FontSize="18" FontWeight="Bold" Margin="120,0,0,0" Text="{Binding ProfitHarian, StringFormat=Rp. {0:N0}}"/>
                        <TextBlock Grid.Column="1" Text="detail" FontSize="12" Foreground="#42A5F5" VerticalAlignment="Center" Cursor="Hand"/>
                    </Grid>
                </Border>

                <!-- KARTU 4: JENIS TANGKAPAN TERBANYAK -->
                <Border Background="White" CornerRadius="10" Padding="20" Effect="{StaticResource ShadowDepth1}">
                    <StackPanel>
                        <TextBlock Text="JENIS TANGKAPAN TERBANYAK (TAHUNAN)" Style="{StaticResource CardTitle}"/>
                        <!-- Placeholder Grafik Batang Kecil -->
                        <Border Height="200" BorderBrush="#eee" BorderThickness="0,0,0,1" Margin="0,0,0,0">
                            <TextBlock Text="[Grafik Batang Simulasi DataJenisTangkapan]" Foreground="#bbb" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <!-- Label X Axis Simulasi -->
                        <Grid Margin="10,5,10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Style="{StaticResource AxisLabel}" Text="Bandeng" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Style="{StaticResource AxisLabel}" Text="Bawal" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Style="{StaticResource AxisLabel}" Text="Tongkol" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Style="{StaticResource AxisLabel}" Text="Trenggiri" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="4" Style="{StaticResource AxisLabel}" Text="Teri" HorizontalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>