<Window x:Class="NelayanGo.InputTangapanWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            mc:Ignorable="d"
            Title="NELAYAN GO - Input Tangkapan" Height="700" Width="1200"
            Background="#F0F0F0">

    <Window.Resources>
        <!-- Style untuk Label Input -->
        <Style TargetType="TextBlock" x:Key="InputLabel">
            <Setter Property="Margin" Value="0,15,0,5"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Style untuk TextBox Input (Diperbaiki: Menggunakan ControlTemplate untuk CornerRadius) -->
        <Style TargetType="{x:Type TextBox}" x:Key="InputField">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border"
								Background="{TemplateBinding Background}"
								BorderBrush="#ddd"
								BorderThickness="1"
								CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#42A5F5"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#1E88E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style untuk efek bayangan sederhana -->
        <DropShadowEffect x:Key="ShadowDepth1" BlurRadius="5" ShadowDepth="2" Direction="270" Opacity="0.1"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- 0: Header -->
            <RowDefinition Height="*"/>
            <!-- 1: Konten Utama -->
        </Grid.RowDefinitions>

        <!-- === HEADER (ROW 0) === -->
        <!-- Perhatikan bahwa StaticResource 'nunito' mungkin masih menyebabkan error 
                jika belum didefinisikan di App.xaml. -->
        <Grid Grid.Row="0" Background="#0361A0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <TextBlock Text="N" FontFamily="Segoe UI" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Text="NELAYAN GO" FontFamily="Segoe UI" Foreground="White" FontSize="20" FontWeight="SemiBold"/>
            </StackPanel>

            <!-- Navigasi -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock x:Name ="HomeLink" Cursor="Hand" MouseLeftButtonDown="HomeLink_MouseLeftButtonDown" Text="Home" Foreground="White" VerticalAlignment="Center" Margin="20,0,20,0"/>
                <TextBlock x:Name ="HasilTangkapanLink" Cursor="Hand" MouseLeftButtonDown="HasilTangkapanLink_MouseLeftButtonDown" Text="Hasil Tangkapan" Foreground="White" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,20,0"/>
                <TextBlock x:Name ="MapsLink" Cursor="Hand" MouseLeftButtonDown="MapsLink_MouseLeftButtonDown" Text="Maps" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBlock x:Name ="HargaPasarLink" Cursor="Hand" MouseLeftButtonDown="HargaPasarLink_MouseLeftButtonDown" Text="Harga Pasar" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
            </StackPanel>

            <!-- Info Pengguna -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,0">
                <!-- Ikon Profil Placeholder
                <Border CornerRadius="50" Width="30" Height="30" Background="White" VerticalAlignment="Center" Margin="0,0,10,0">
                    <TextBlock Text="👤" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                </Border> -->
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="FIKRI HOIRUL" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Text="TGL-32" Foreground="White" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- === KONTEN UTAMA (ROW 1) === -->
        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- Kolom Form -->
                <ColumnDefinition Width="250"/>
                <!-- Kolom Jam -->
            </Grid.ColumnDefinitions>

            <!-- KIRI: FORM INPUT TANGKAPAN -->
            <Grid Grid.Column="0" Margin="0,0,30,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="80"/>
                    <!-- Baris Tombol -->
                </Grid.RowDefinitions>

                <!-- KARD FORM -->
                <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="30" Effect="{StaticResource ShadowDepth1}">
                    <StackPanel>
                        <TextBlock Text="INPUT TANGKAPAN" FontSize="20" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>

                        <!-- Field Nama Ikan -->
                        <TextBlock Style="{StaticResource InputLabel}" Text="Nama Ikan *"/>
                        <TextBox Style="{StaticResource InputField}" Text="Bandeng"/>

                        <!-- Field Berat Ikan -->
                        <TextBlock Style="{StaticResource InputLabel}" Text="Berat Ikan (kg) *"/>
                        <TextBox Style="{StaticResource InputField}" Text="1.25"/>

                        <!-- Field Jam Tangkap -->
                        <TextBlock Style="{StaticResource InputLabel}" Text="Jam Tangkap *"/>
                        <TextBox Style="{StaticResource InputField}" Text="19:00-20:00"/>

                        <!-- Field Tanggal Tangkap -->
                        <TextBlock Style="{StaticResource InputLabel}" Text="Tanggal Tangkap *"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <!-- DatePicker digunakan untuk input tanggal -->
                            <!-- Perlu styling tambahan pada DatePicker jika ingin sudut membulat -->
                            <DatePicker Grid.Column="0" SelectedDate="2001-01-01" Margin="0" Padding="10,8" BorderBrush="#ddd" BorderThickness="1" Background="White" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="📅" FontSize="20" VerticalAlignment="Center" Margin="-35,0,10,0"/>
                        </Grid>

                        <!-- Field Lokasi -->
                        <TextBlock Style="{StaticResource InputLabel}" Text="Lokasi *"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Style="{StaticResource InputField}" Text="-8.033341638731791, 110.18654457227322"/>
                            <TextBlock Grid.Column="1" Text="📍" FontSize="20" VerticalAlignment="Center" Margin="-35,0,10,0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- BARIS TOMBOL -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <!-- Tombol BATAL (Merah) -->
                    <Button Content="BATAL" Width="150" Height="45" FontSize="16" FontWeight="SemiBold" Click="BatalButton_Click">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}">
                                <Setter Property="Background" Value="#E53935"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type Button}">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#C62828"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Tombol KIRIM (Biru) -->
                    <Button Content="KIRIM" Width="150" Height="45" FontSize="16" FontWeight="SemiBold" Margin="20,0,0,0" Click="KirimButton_Click">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}">
                                <Setter Property="Background" Value="#42A5F5"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type Button}">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1E88E5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- KANAN: WIDGET JAM DAN TANGGAL -->
            <Border Grid.Column="1" Width="200" Height="100" CornerRadius="10" Background="White" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="15">
                <StackPanel>
                    <!-- Jam -->
                    <TextBlock x:Name="TimeTextBlock"
							Text="--:--:--"
							FontSize="30"
							FontWeight="Bold"
							HorizontalAlignment="Center"
							Foreground="#333"/>

                    <!-- Tanggal -->
                    <TextBlock x:Name="DateTextBlock"
							Text="--/--/----"
							FontSize="14"
							HorizontalAlignment="Center"
							Foreground="#666"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>